# Portfolio-Sharpe-Calculator
These Python Code receives stocks and weights of an portfolio as input and returns the Sharpe Index, covariance, variance, portfolio volatility &amp; expected return  as outputs
